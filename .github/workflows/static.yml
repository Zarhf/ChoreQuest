name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying static content
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Generate Config
        run: |
          echo "const CONFIG = {
              BUILD: $(cat version.json | jq -r .build),
              VAPID_PUBLIC_KEY: \"${{ secrets.VAPID_PUBLIC_KEY }}\",
              firebase: {
                  apiKey: \"${{ secrets.FIREBASE_API_KEY }}\",
                  authDomain: \"${{ secrets.FIREBASE_AUTH_DOMAIN }}\",
                  projectId: \"${{ secrets.FIREBASE_PROJECT_ID }}\",
                  storageBucket: \"${{ secrets.FIREBASE_STORAGE_BUCKET }}\",
                  messagingSenderId: \"${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}\",
                  appId: \"${{ secrets.FIREBASE_APP_ID }}\",
                  measurementId: \"${{ secrets.FIREBASE_MEASUREMENT_ID }}\"
              }
          };" > js/config.js
      - name: Update Service Worker
        run: |
          sed -i "s/{{FIREBASE_API_KEY}}/${{ secrets.FIREBASE_API_KEY }}/g" firebase-messaging-sw.js
          sed -i "s/{{FIREBASE_AUTH_DOMAIN}}/${{ secrets.FIREBASE_AUTH_DOMAIN }}/g" firebase-messaging-sw.js
          sed -i "s/{{FIREBASE_PROJECT_ID}}/${{ secrets.FIREBASE_PROJECT_ID }}/g" firebase-messaging-sw.js
          sed -i "s/{{FIREBASE_STORAGE_BUCKET}}/${{ secrets.FIREBASE_STORAGE_BUCKET }}/g" firebase-messaging-sw.js
          sed -i "s/{{FIREBASE_MESSAGING_SENDER_ID}}/${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}/g" firebase-messaging-sw.js
          sed -i "s/{{FIREBASE_APP_ID}}/${{ secrets.FIREBASE_APP_ID }}/g" firebase-messaging-sw.js
          sed -i "s/{{FIREBASE_MEASUREMENT_ID}}/${{ secrets.FIREBASE_MEASUREMENT_ID }}/g" firebase-messaging-sw.js
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4